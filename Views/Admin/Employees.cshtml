@using EInsurance_App.ViewModels.Common
@model PagedResult<EInsurance_App.Models.Employee>

<div class="ei-page-header">
    <div class="header-icon"><i class="bi bi-briefcase"></i></div>
    <h2>All Employees</h2>
</div>

<div class="mb-3">
    <a asp-action="CreateEmployee" class="btn btn-primary"><i class="bi bi-plus-lg me-1"></i>Add Employee</a>
</div>

@if (Model != null && Model.Data.Any())
{
    <div class="ei-table table-responsive">
        <table class="table mb-0">
            <thead>
                <tr><th>ID</th><th>Username</th><th>Email</th><th>Full Name</th><th>Role</th><th>Actions</th></tr>
            </thead>
            <tbody>
                @foreach (var e in Model.Data)
                {
                    <tr>
                        <td><span class="badge" style="background:#e0f2fe;color:#0369a1;">#@e.EmployeeId</span></td>
                        <td>@e.Username</td>
                        <td>@e.Email</td>
                        <td><strong>@e.FullName</strong></td>
                        <td><span class="badge" style="background:#ede9fe;color:#7c3aed;">@e.Role</span></td>
                        <td>
                            <a asp-action="EditEmployee" asp-route-id="@e.EmployeeId" class="btn btn-warning btn-sm me-1">
                                <i class="bi bi-pencil me-1"></i>Edit
                            </a>
                            <a asp-action="DeleteEmployee" asp-route-id="@e.EmployeeId" class="btn btn-danger btn-sm"
                               onclick="return confirm('Delete this employee?');">
                                <i class="bi bi-trash me-1"></i>Delete
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <nav class="mt-4">
        <ul class="pagination justify-content-center">
            <li class="page-item @(Model.PageNumber == 1 ? "disabled" : "")">
                <a class="page-link" asp-action="Employees" asp-route-page="@(Model.PageNumber - 1)">&#171; Prev</a>
            </li>
            @for (int i = 1; i <= Model.TotalPages; i++)
            {
                <li class="page-item @(i == Model.PageNumber ? "active" : "")">
                    <a class="page-link" asp-action="Employees" asp-route-page="@i">@i</a>
                </li>
            }
            <li class="page-item @(Model.PageNumber == Model.TotalPages ? "disabled" : "")">
                <a class="page-link" asp-action="Employees" asp-route-page="@(Model.PageNumber + 1)">Next &#187;</a>
            </li>
        </ul>
    </nav>
    <p class="text-center text-muted small">Page <strong>@Model.PageNumber</strong> of <strong>@Model.TotalPages</strong> &mdash; Total: <strong>@Model.TotalRecords</strong></p>
}
else
{
    <div class="alert alert-info text-center"><i class="bi bi-info-circle me-1"></i>No employees found.</div>
}
